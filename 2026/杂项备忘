1、MySQL定时备份：脚本创建备份文件 + cron定时执行任务
2、阿里云默认无法访问外网镜像，可以用：容器镜像服务 → 镜像工具 → 镜像加速器
3、幂等性：对同一个系统，使用同样的条件，一次请求和多次重复请求对系统资源的影响是一样的：比如同一个订单支付一次和支付10次结果是一样的
4、lock 底层使用 Monitor，Monitor 记录锁的线程所有权，通常要求获取锁和释放锁由同一个线程完成。await 可能让异步方法暂停，恢复时不保证仍由同一个线程继续执行，因此 C# 禁止在 lock 语句体内使用 await。
    而SemaphoreSlim.WaitAsync 是异步等待，不会阻塞线程，而且它管理的是许可而不是线程所有权，因此适合保护包含异步 I/O 的临界区。拿到许可后必须使用 try/finally 调用 Release，否则后续任务会永久等待。